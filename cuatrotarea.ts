let personas: string[] = ['Ashlee','Soledispa','Gabriel'];
let salario: number[] = [450, 1200, 1000];

for ( let i = 0; i < personas.length; i++ ){
  let personas = personas[i];
  let salario = salario[i];
  
  console.log( personas + ' tiene un salario de '+ salario);
}
