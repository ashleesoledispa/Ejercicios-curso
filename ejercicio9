interface person {
  name: string;
  age: number;
  isActive boolean;
}
  
let ashlee: person = {
  nombre: 'Ashlee',
  edad: 18
  isActive: true,
};

let dayanna: person = {
  nombre: 'Dayanna',
  edad: 26,
  isActive: true,
};

let personas: person = [ashlee, dayanna];

for( let i = 0; i < personas.length; i++){
  let person = personas[i];
  console.log(person.name + ' ' + person.age);
}
